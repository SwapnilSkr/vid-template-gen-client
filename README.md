# Reel Review Client

Vite + React 19 review studio for Reddit reel creation, preview, metadata editing,
thumbnail review, download, and YouTube Shorts publishing.

## Quick Start

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` to point at the backend API (defaults to
`http://localhost:3000/api`).
